{
  "name": "text-visualizer",
  "version": "1.0.0",
  "description": "Text visualizer for L10N rendering",
  "scripts": {
    "build": "bun build public/textParser.ts --outfile public/textParser.js --target browser --format esm && bun build public/app.ts --outfile public/app.js --target browser --format esm && bun build public/tooltip.ts --outfile public/tooltip.js --target browser --format esm",
    "dev": "bun run build && bun run server.ts",
    "start": "bun run build && bun run server.ts"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.3.3"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^7.3.6",
    "@popperjs/core": "^2.11.8"
  }
}
